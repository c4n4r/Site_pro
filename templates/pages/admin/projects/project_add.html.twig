{% extends 'pages/admin/admin_base.html.twig' %}

{% block body %}
    <h2 class="text-dark mb-5">nouvelle techno</h2>

    {{ form_start(projectForm) }}
    <div class="flex flex-row">
        <div class="flex w-2/5 flex-col">
            <div class="flex my-2">
                {{ form_label(projectForm.name, null,  {'label_attr' : {'class' : 'text-golden mr-3'}}) }}
                {{ form_widget(projectForm.name , { 'attr': { 'class' : 'bg-light rounded shadow p-2'}}) }}
            </div>
            <div class="flex h-24 my-2">
                {{ form_label(projectForm.description, null,  {'label_attr' : {'class' : 'text-golden mr-3'}}) }}
                {{ form_widget(projectForm.description , { 'attr': { 'class' : 'bg-light rounded shadow p-2'}}) }}
            </div>
            <div class="flex my-2">
                {{ form_label(projectForm.skills, null,  {'label_attr' : {'class' : 'text-golden mr-3'}}) }}
                {{ form_widget(projectForm.skills , { 'attr': { 'class' : 'bg-light rounded shadow p-2 px-4'}}) }}
            </div>
            <div class="flex items-center my-2">
                {{ form_label(projectForm.image, null,  {'label_attr' : {'class' : 'text-golden mr-3'}}) }}
                <div>
                    <div class="flex w-full items-center justify-center bg-grey-lighter">
                        <label class="flex w-full flex-col items-center px-4 py-2 bg-white text-blue rounded-lg shadow-lg tracking-wide uppercase border border-blue cursor-pointer hover:bg-blue hover:text-white">
                            <span class="text-base text-golden leading-normal">Select a file</span>
                            {{ form_widget(projectForm.image , { 'attr': { 'class' : 'hidden image-input' }}) }}
                        </label>
                    </div>
                </div>
            </div>
            <div class="flex mt-6">
                {{ form_widget(projectForm.save, {'attr': {'class' : 'button button-dark' }}) }}
            </div>
        </div>
        <div class="flex ml-6 w-48 h-48 rounded-lg border items-center justify-center">
            <span name="no-img-span" class="text-golden">No Image</span>
            <img class="rounded" src="" id="preview" alt="">
        </div>
    </div>

    {{ form_end(projectForm) }}

{% endblock %}


{% block customJS %}
    {{ encore_entry_script_tags('custom') }}
{% endblock %}